management:
  endpoints:
    web:
      exposure:
        include: "health,info"
  info:
    java:
      enabled: true
    env:
      enabled: true

info:
  app:
    version: '@revision@'

endpoint:
  post:
    url: "http://localhost:8090"
  connection:
    url: "http://localhost:8090"
  user:
    url: "http://localhost:8090"
